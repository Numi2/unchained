[package]
name = "checkpoint-accumulator-method"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
proof-core = { path = "../../proof-core" }
risc0-zkvm = { version = "3.0.5", default-features = false, features = ["std"] }
